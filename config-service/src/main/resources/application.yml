server:
  port: 8888
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Maducdornelles/microservices-java
          search-paths:
          - microservices-java/configs/{application}
          #username: Maducdornelles
          #password: senha